# init_config:

# instances:
#   - dbm: true
#     host: ssql,1433
#     username: sa
#     password: Password1!
#     connector: odbc
#     driver: ODBC Driver 18 for SQL Server
#     connection_string: "TrustServerCertificate=yes;"

init_config:

instances:
  - dbm: true
    host: 'ssql,1433'
    username: sa
    password: Password1!
    connector: odbc
    driver: FreeTDS
